resources:

  pipelines:
    # DLT pipeline..
    b_medium_metrics_pipeline:
      name: "test dlt pipe"
      target: "b_medium_post_report"
      libraries:
        - file:
            path: ./rhelp/ingest.py
      channel: preview
      configuration:
        "bundle.file_path": "/Workspace/${workspace.file_path}"   
