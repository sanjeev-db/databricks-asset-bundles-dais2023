resources:

  pipelines:
    # DLT pipeline..
    a_medium_metrics_pipeline:
      name: "test dlt pipe"
      target: "a_medium_post_report"
      libraries:
        - file:
            path: ./rhelp/ingest.py
      channel: preview
      configuration:
        "bundle.file_path": "/Workspace/${workspace.file_path}"   
