resources:

  pipelines:
    # DLT pipeline..
    generic_dlt_pipeline:
      name: "[${bundle.environment}] rhelp Medium Metrics Pipeline"
      target: "rhelp_medium_post_report_${bundle.environment}"
      libraries:
        - file:
            path: ./generic-dlt-demo/02-Retail_DLT_CDC_Python_Metadriven.py
      channel: preview
      configuration:
        "bundle.file_path": "/Workspace/${workspace.file_path}"    