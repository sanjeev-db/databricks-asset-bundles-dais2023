resources:

  pipelines:
    # DLT pipeline..
    generic_dlt_pipeline:
      name: "[${bundle.environment}] Generic DLT Demo Pipeline"
      target: "dlt_demo_generic_${bundle.environment}"
      libraries:
        - notebook: 
              path: ./generic-dlt-demo/02-Retail_DLT_CDC_Python_Metadriven.py
      channel: preview
      configuration:
        "bundle.file_path": "/Workspace/${workspace.file_path}"    