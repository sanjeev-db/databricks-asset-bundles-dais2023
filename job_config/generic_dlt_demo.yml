resources:

  pipelines:
    # DLT pipeline..
    medium_metrics_pipeline1111:
      name: "[${bundle.environment}] rhelp Medium Metrics Pipeline1111"
      target: "rhelp_medium_post_report1111_${bundle.environment}"
      libraries:
        - file:
            path: ./rhelp/ingest.py
        - file:
            path: ./rhelp/get_metrics.py
      channel: preview
      configuration:
        "bundle.file_path": "/Workspace/${workspace.file_path}"   